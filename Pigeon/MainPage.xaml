<phone:PhoneApplicationPage
    x:Class="people_hub.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="PortraitOrLandscape" Orientation="Portrait"
    shell:SystemTray.IsVisible="True">

    <!--LayoutRoot is the root grid where all page content is placed-->
    <Grid x:Name="LayoutRoot" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- LOCALIZATION NOTE:
            To localize the displayed strings copy their values to appropriately named
            keys in the app's neutral language resource file (AppResources.resx) then
            replace the hard-coded text value between the attributes' quotation marks
            with the binding clause whose path points to that string name.

            For example:

                Text="{Binding Path=LocalizedResources.ApplicationTitle, Source={StaticResource LocalizedStrings}}"

            This binding points to the template's string resource named "ApplicationTitle".

            Adding supported languages in the Project Properties tab will create a
            new resx file per language that can carry the translated values of your
            UI strings. The binding in these examples will cause the value of the
            attributes to be drawn from the .resx file that matches the
            CurrentUICulture of the app at run time.
         -->

        <!--TitlePanel contains the name of the application and page title-->
        <StackPanel x:Name="TitlePanel" Grid.Row="0" Margin="12,17,0,28">
            <TextBlock Text="Pigeon" Margin="0" Style="{StaticResource PhoneTextTitle1Style}" RenderTransformOrigin="0.49,2.094" Height="114"/>
            <TextBlock Margin="0" Style="{StaticResource PhoneTextTitle1Style}" FontSize="24" Height="78">
            	<Run Text="你好，"/>
            	<Run Text="欢迎使用Pigeon"/>
            	<LineBreak/>
            	<Run Text="首次使用请设定你的用户信息"/>
            	<LineBreak/>
            </TextBlock>
        </StackPanel>

        <!--ContentPanel - place additional content here-->
        <Grid x:Name="ContentPanel" Grid.Row="1" Margin="0">
            <ScrollViewer Margin="0,10,0,67">
                <Grid Height="372" Width="381" VerticalAlignment="Top">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="25*"/>
                        <RowDefinition Height="25*"/>
                        <RowDefinition Height="40*"/>
                        <RowDefinition Height="40*"/>
                        <RowDefinition Height="40*"/>
                        <RowDefinition Height="40*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Margin="0" TextWrapping="Wrap" Text="Phone Number" HorizontalAlignment="Left"/>
                    <TextBlock x:Name="number"  Grid.Row="1" Margin="0" TextWrapping="Wrap" Text="18668006648" HorizontalAlignment="Left"/>
                    <Grid Grid.Row="2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="190*"/>
                            <ColumnDefinition Width="191*"/>
                        </Grid.ColumnDefinitions>
                        <TextBox x:Name="firstname" Grid.Column="0" HorizontalAlignment="Stretch" Height="Auto" TextWrapping="Wrap" Text="FirstName" Width="Auto"/>
                        <TextBox x:Name="lasttname" Grid.Column="1" HorizontalAlignment="Stretch" Height="Auto" TextWrapping="Wrap" Text="LastName" Width="Auto"/>
                    </Grid>
                    <TextBox x:Name="email" Grid.Row="3" HorizontalAlignment="Stretch" Height="Auto" TextWrapping="Wrap" Text="Email" Width="Auto"/>
                    <TextBox x:Name="password" Grid.Row="4" HorizontalAlignment="Stretch" Height="Auto" TextWrapping="Wrap" Text="Password" Width="Auto"/>
                    <Button x:Name="register" Grid.Row="5" Content="注册" Click="register_Click"/>
                </Grid>
            </ScrollViewer>
        </Grid>

        <!--Uncomment to see an alignment grid to help ensure your controls are
            aligned on common boundaries.  The image has a top margin of -32px to
            account for the System Tray. Set this to 0 (or remove the margin altogether)
            if the System Tray is hidden.

            Before shipping remove this XAML and the image itself.-->
        <!--<Image Source="/Assets/AlignmentGrid.png" VerticalAlignment="Top" Height="800" Width="480" Margin="0,-32,0,0" Grid.Row="0" Grid.RowSpan="2" IsHitTestVisible="False" />-->
    </Grid>

</phone:PhoneApplicationPage>